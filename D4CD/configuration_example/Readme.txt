Configuration file is used to user specify D4CD driver standard (defaults)  sizes, properties of individual object and other behaviour of driver.

Here is example file that has to be added to user application code base, and thus allows individual application settings.